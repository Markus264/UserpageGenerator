public class TeammatePlacements {
    double avgPosition;
    ProcessedPlayer parent;

    public TeammatePlacements(double avgPosition, ProcessedPlayer parent)
    {
        this.avgPosition = avgPosition;
        this.parent = parent;
    }
}
